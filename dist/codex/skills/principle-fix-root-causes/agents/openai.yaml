interface:
  display_name: "Pstack: Principle Fix Root Causes"
  short_description: "Invoke this pstack workflow explicitly."
  default_prompt: "Use $pstack:principle-fix-root-causes for this task."
policy:
  allow_implicit_invocation: false
